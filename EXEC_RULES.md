# 开发与执行铁律 (Execution Rules)

> **核心声明**：每次接收到用户指令并执行任何操作或修改前，必须先严格对照本规则执行。

---

## 一、功能改动红线（核心第一条）

1. **没要求改功能，不得擅自改！**
   - 严禁借着“优化”、“重构”、“清理代码”、“规范化”等名义，私自修改已有功能的业务逻辑、数据流向、默认参数或现有交互。
   - 遇到用户未明确指出的既有代码或未通过的测试，不得在未经用户明确授权的情况下自行修改业务代码来强行使其适配。
   - 需求范围外的多余发挥即视为 Bug。

---

## 二、任务执行与修改准则

1. **精准定位，最小化修改**：
   - 严格限定在用户明确指示的文件、函数、样式或配置范围内操作。
   - 严禁扩大修改面或顺带修改不相关的代码文件。

2. **先看先查，禁止臆测**：
   - 操作前先核对实际现有实现，确认现有逻辑与接口约定，不凭记忆或文件名猜测代码。

3. **改动即提交并上传 GitHub（便于回滚）**：
   - 每次完成文件改动与验证后，必须通过 Git 提交（commit）并推送到 GitHub 对应仓库分支（push），确保改动有迹可循、随时可安全回滚。

4. **操作透明与安全**：
   - 涉及删除文件、覆写配置或重要操作前，必须向用户明确确认。
   - 保持代码审查友好，每次修改确保改动干净、意图清晰。
