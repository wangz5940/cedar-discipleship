---
alwaysApply: true
---

# Trae 项目规则

开始任务前读取并遵循仓库根目录 `AGENTS.md`，该文件是所有 Agent 共用的开发与发布规则。

部署版本必须先合入 `master`，并在发布前验证目标提交已包含在最新 `origin/master` 历史中。所有变更经功能分支、PR 和必需 CI 合入，禁止绕过分支保护。

其他贡献者的未合入修复由原作者提交；未经用户明确授权，不得代为提交或整合。
